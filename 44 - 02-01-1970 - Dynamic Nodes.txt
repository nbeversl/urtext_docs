
Dynamic Nodes - OLD
ID::qjl



                Note that in order to work, the target node must exist; dynamic definitions do not create their target nodes. 


{    Example 1 : List

    Here are four inline nodes with example tags and indexes:

        { Example Source Node 1
         
ID::017
tag::example_node
flags::exclude_from_tree
index::02 }
                               
        { Example Source Node 3
           
ID::01b
tag::example_node
flags::exclude_from_tree
index::01 }

        { Example Source Node 2
        
tag::example_node | exclude_this 
ID::00v
flags::exclude_from_tree }

        { Example Source Node 4
           
ID::01c
tag::example_node
index::05
flags::exclude_from_tree }

    ( Note that each of these nodes has the `exclude_from_tree` flag in the metadata, so that they are not included in the Table of Contents. See >1e3. )

    Here is an example dynamic node definition targeting node ID >016

        [[ ID(016)
            INCLUDE(tag=example_node)
            EXCLUDE( tag=exclude_this )
            SORT(index)
            FOOTER(title::Example Dynamic Node Title
                flags::exclude_from_tree
                )
        ]] >016

        Here is the compiled node defined by the definition above. Changing the dynamic definition and/or the contents or metadata of the source nodes will update the dynamic node. Saving is necessary to trigger the update.


                                                                                              


ID::01r
Timestamp::<Sat., Jun. 08, 2019, 10:15 AM>
index::08}

{ Example 2 : Tree 

    Here is the dynamic definition that generates the table of contents for this documentation:

        [[  ID(>e5j)
            +(ID=013) +(ID=z7t) +(ID=ac5) -(tags=exclude_from_export)
            DEPTH(20)
            FOOTER(title::Table of Contents)
            EXPORT(f>README.md -markdown -preformat)
         ]]  

    Changing the titles or structure of sections in this documentation will dynamically update the table of contents tree.

    For more information on trees, see |  ?  >00h

     
ID::029}

 
ID::ri9
    